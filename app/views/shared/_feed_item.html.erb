<li id="<%= feed_item.id %>">

    <span class="user">
      <%= link_to feed_item.user.name, feed_item.user %>
    </span>
    <span class="bookmarkURL"><%= feed_item.url %></span>
  <% if current_user?(feed_item.user) %>
    <%= link_to "delete", feed_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feed_item.name %>
  <% end %>
</li>
